import { isNthItem } from 'map-app/helpers/is-nth-item';

module('IsNthItemHelper');

// Replace this with your real tests.
test('it works', function() {
  var result = isNthItem(42);
  ok(result);
});
